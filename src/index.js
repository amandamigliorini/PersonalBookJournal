import App from './App';

//get the root div (the only element inside the body) and add all elements created on the App function (header, main and footer).
document.getElementById('root').appendChild(App());